"use client";

/**
 * No admin donations-list endpoint exists yet — add GET /api/admin/donations in matchids-backend.
 */
export default function DonationsPage() {
  return (
    <div className="p-8">
      <h1 className="font-display text-2xl font-bold text-ink">Donations</h1>
      <p className="mt-2 max-w-xl text-sm text-ink/60">No admin donations-list endpoint exists yet — add GET /api/admin/donations in matchids-backend.</p>
    </div>
  );
}
