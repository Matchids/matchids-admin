"use client";

/**
 * No settings model exists yet — this screen is a placeholder until platform-wide config (e.g. active payment providers) has somewhere to live.
 */
export default function SettingsPage() {
  return (
    <div className="p-8">
      <h1 className="font-display text-2xl font-bold text-ink">Platform Settings</h1>
      <p className="mt-2 max-w-xl text-sm text-ink/60">No settings model exists yet — this screen is a placeholder until platform-wide config (e.g. active payment providers) has somewhere to live.</p>
    </div>
  );
}
