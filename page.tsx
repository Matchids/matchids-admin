"use client";

/**
 * No admin users-list endpoint exists yet in matchids-backend — add GET /api/admin/users there first.
 */
export default function UsersPage() {
  return (
    <div className="p-8">
      <h1 className="font-display text-2xl font-bold text-ink">Users</h1>
      <p className="mt-2 max-w-xl text-sm text-ink/60">No admin users-list endpoint exists yet in matchids-backend — add GET /api/admin/users there first.</p>
    </div>
  );
}
