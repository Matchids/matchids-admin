"use client";

/**
 * GET /api/artwork exists; admin create/edit endpoints are an integration point for matchids-backend.
 */
export default function KidsArtPage() {
  return (
    <div className="p-8">
      <h1 className="font-display text-2xl font-bold text-ink">Kids Art</h1>
      <p className="mt-2 max-w-xl text-sm text-ink/60">GET /api/artwork exists; admin create/edit endpoints are an integration point for matchids-backend.</p>
    </div>
  );
}
