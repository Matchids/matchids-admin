"use client";

/**
 * No admin payments-list endpoint exists yet — add GET /api/admin/payments in matchids-backend.
 */
export default function PaymentsPage() {
  return (
    <div className="p-8">
      <h1 className="font-display text-2xl font-bold text-ink">Payments</h1>
      <p className="mt-2 max-w-xl text-sm text-ink/60">No admin payments-list endpoint exists yet — add GET /api/admin/payments in matchids-backend.</p>
    </div>
  );
}
