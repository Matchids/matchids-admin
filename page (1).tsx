"use client";

/**
 * No admin orders-list endpoint exists yet — add GET /api/admin/orders in matchids-backend.
 */
export default function OrdersPage() {
  return (
    <div className="p-8">
      <h1 className="font-display text-2xl font-bold text-ink">Orders</h1>
      <p className="mt-2 max-w-xl text-sm text-ink/60">No admin orders-list endpoint exists yet — add GET /api/admin/orders in matchids-backend.</p>
    </div>
  );
}
